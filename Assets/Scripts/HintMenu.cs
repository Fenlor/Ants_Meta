using UnityEngine;

public class HintMenu : MonoBehaviour
{
    private bool doShowMenu = true;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //if (doShowMenu)
        //{
        //    transform.position = OVRInput.GetLocalControllerPosition(OVRInput.Controller.LTouch);
        //    transform.rotation = OVRInput.GetLocalControllerRotation(OVRInput.Controller.LTouch);

        //    Debug.DrawRay(transform.position, transform.rotation * Vector3.forward * 5, Color.red);
        //}
    }
}
